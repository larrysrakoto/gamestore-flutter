import 'package:flutter/material.dart';

class HeaderSections extends StatelessWidget {
  const HeaderSections({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      color: Colors.red,
    );
  }
}
